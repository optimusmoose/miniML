package config;


/**
 * A lazy config class for the Messaging system.
 * TODO: extract to a .properties file in src/main/resources
 */
public class MessageingConfig {
    public static final String APPNAME = "miniML";
}
